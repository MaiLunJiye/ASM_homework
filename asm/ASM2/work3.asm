DATAS SEGMENT
DATAS ENDS

STACKS SEGMENT
STACKS ENDS

CODES SEGMENT
	ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
	MOV BH,1
	MOV DX,0646H
	MOV AL,10000000B;
	OUT DX,AL
	MOV DX,0640H
	MOV AL,11111111B
AA:
	call show
	JMP AA
	
show:
	push DX
	push AX
	mov al,bl
	mov dx,0640h
	out dx,al
	pop AX
	pop DX
	ret
CODES ENDS
	END START