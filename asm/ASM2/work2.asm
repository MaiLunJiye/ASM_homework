DATAS SEGMENT
DATAS ENDS

STACKS SEGMENT
STACKS ENDS

CODES SEGMENT
	ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
	MOV DX,06C6H
	MOV AL,00010110B; ini count 1
	OUT DX,AL
	
	MOV AL,01010110B; ini count 2
	OUT DX,AL

	MOV AL,180	
	MOV DX,06C0H
	OUT DX,AL
	
	MOV AL,100
	MOV DX,06C2H
	OUT DX,AL
	nop
	nop
	nop

CODES ENDS
	END START